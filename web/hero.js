'use strict';
(()=>{const video=document.getElementById('hero-video'),button=document.getElementById('hero-motion');if(!video||!button)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)');let pausedByUser=reduced.matches;const sync=()=>{button.textContent=video.paused?'Play animation':'Pause animation';button.setAttribute('aria-label',button.textContent);};const play=()=>video.play().then(sync).catch(sync);if(pausedByUser){video.autoplay=false;video.pause();}else play();button.onclick=()=>{pausedByUser=!video.paused;if(pausedByUser)video.pause();else play();sync();};video.addEventListener('play',sync);video.addEventListener('pause',sync);document.addEventListener('visibilitychange',()=>{if(document.hidden)video.pause();else if(!pausedByUser)play();});reduced.addEventListener('change',e=>{pausedByUser=e.matches;if(pausedByUser)video.pause();else play();sync();});sync();})();
