'use strict';
(()=>{const video=document.getElementById('hero-video');if(!video)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)');video.controls=false;video.disablePictureInPicture=true;const play=()=>{if(!document.hidden&&!reduced.matches)video.play().catch(()=>{});};if(reduced.matches){video.autoplay=false;video.pause();}else play();document.addEventListener('visibilitychange',()=>{if(document.hidden)video.pause();else play();});reduced.addEventListener('change',e=>{if(e.matches)video.pause();else play();});document.addEventListener('pointerdown',play,{once:true});})();
